# =============================================================================
# MBA Deobfuscator Dependencies
# =============================================================================
#
# RECOMMENDED: Use setup.sh (Linux/cloud) or setup.ps1 (Windows) for automatic
# CUDA detection and proper PyTorch/PyG installation.
#
# For manual installation:
#   1. Install PyTorch: https://pytorch.org/get-started/locally/
#   2. Install PyG extensions: https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
#   3. pip install -r requirements.txt
#
# =============================================================================

# Note: torch, torchvision, torch-geometric, torch-scatter, torch-sparse
# must be installed separately with matching CUDA versions.
# See setup.sh for automatic installation.

# Core dependencies
numpy>=1.24.0
z3-solver>=4.12.0
pybind11>=2.11.0

# Training & monitoring
tensorboard>=2.13.0
wandb>=0.15.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
lark>=1.1.0

# Testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
